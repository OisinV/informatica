tumblehouse.py makes an image of a house tumbling, *efficiently*.  
  
-mr is the most readable, and -pr is my preffered amount of readable, -test is often empty and just a test.
  
command:  
```bash  
py tumblehouse-mr.py && py tumblehouse-pr.py && tumblehouse-test.py  
```  
